# Sentinel — configuración del backend (back/).
#
# Copia este archivo a `.env` (en esta misma carpeta) y completa los
# valores. `.env` está en .gitignore — nunca se commitea.

PORT=3000
# Orígenes permitidos para CORS (Flutter Web corre en su propio origen).
# Varios valores separados por coma. "*" en dev si no quieres pensarlo.
CORS_ORIGIN=*

# Postgres del proyecto Supabase — botón "Connect" arriba en el dashboard
# del proyecto → pestaña "URI" (o Project Settings → Database → Connection
# string). No requiere Docker, es una conexión normal a un Postgres remoto.
DATABASE_URL=postgresql://postgres:TU_PASSWORD@TU_PROYECTO.supabase.co:5432/postgres

# Botón "Connect" arriba en el dashboard, o Project Settings → API Keys.
# Usadas ÚNICAMENTE por back/src/routes/auth.routes.ts para hablar con
# Supabase Auth en nombre del front (registro/login/refresh/logout) — el
# front no tiene ninguna de estas dos credenciales (docs/architecture.md).
SUPABASE_URL=https://TU_PROYECTO.supabase.co
SUPABASE_PUBLISHABLE_KEY=REEMPLAZA_CON_TU_SUPABASE_PUBLISHABLE_KEY

# NO hace falta tocar esto en un proyecto nuevo (creado desde nov. 2025):
# `middleware/auth.ts` verifica el token con las llaves públicas de
# Supabase (JWKS, vía SUPABASE_URL de arriba) sin necesitar ningún secreto
# extra. Deja esta línea vacía/comentada.
#
# Solo la necesitas si tu proyecto es viejo y todavía usa el "JWT Secret"
# clásico (Project Settings → API Keys → JWT Settings → "JWT Secret") —
# en ese caso, descoméntala y pégalo ahí.
# SUPABASE_JWT_SECRET=

# --- Despacho de alertas de accidente (opcional) ---
# Sin configurar, cada canal cae a un proveedor "Noop": el pipeline corre
# igual (se registra el intento en `alerts` con status='failed' y un motivo
# claro), solo que no se envía nada de verdad. Ver back/src/alerts/.

# Firebase Cloud Messaging — JSON completo de la cuenta de servicio
# (Firebase Console → Configuración del proyecto → Cuentas de servicio →
# Generar nueva clave privada), como una sola línea.
FCM_SERVICE_ACCOUNT_JSON=

# Twilio (SMS)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Meta WhatsApp Cloud API
META_WHATSAPP_ACCESS_TOKEN=
META_WHATSAPP_PHONE_NUMBER_ID=
META_WHATSAPP_TEMPLATE_NAME=accident_alert
META_WHATSAPP_TEMPLATE_LANG=es
